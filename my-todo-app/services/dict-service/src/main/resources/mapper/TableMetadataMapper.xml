<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dict.mapper.TableMetadataMapper">

    <select id="listTables" resultType="com.example.dict.dto.TableInfoDTO">
        SELECT TABLE_NAME AS tableName,
               TABLE_COMMENT AS tableComment,
               TABLE_SCHEMA AS tableSchema
        FROM INFORMATION_SCHEMA.TABLES
        WHERE TABLE_TYPE = 'BASE TABLE'
          AND TABLE_SCHEMA IN
          <foreach collection="schemas" item="s" open="(" separator="," close=")">
              #{s}
          </foreach>
        ORDER BY TABLE_SCHEMA, TABLE_NAME
    </select>

    <select id="listColumns" resultType="com.example.dict.dto.TableColumnDTO">
        SELECT COLUMN_NAME AS columnName,
               DATA_TYPE AS dataType,
               COLUMN_TYPE AS columnType,
               COLUMN_COMMENT AS columnComment,
               IS_NULLABLE AS isNullable,
               COLUMN_KEY AS columnKey,
               COLUMN_DEFAULT AS columnDefault
        FROM INFORMATION_SCHEMA.COLUMNS
        WHERE TABLE_SCHEMA = #{schema}
          AND TABLE_NAME = #{tableName}
        ORDER BY ORDINAL_POSITION
    </select>

</mapper>
